@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Rejestracja";
}

<h1>@ViewData["Title"]</h1>
<h4>Połącz swoje konto z kontem @Model.LoginProvider.</h4>
<hr />

<p class="text-info">
   Udanie połączyłeś swoje konto z kontem <strong>@Model.LoginProvider</strong>.
    Proszę wpisz swój email i naciśnij przycisk "Rejestracja", aby zakończyć proces logowania.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Rejestracja</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
